import "./geometry/point"
import "./geometry/shapes"

a = Point.new(x: 0.0, y: 0.0)
b = Point.new(x: 3.0, y: 4.0)

puts("Points:")
puts("  a = #{a.to_s}")
puts("  b = #{b.to_s}")
puts("  distance a->b = #{a.distance_to(b)}")
puts("  b translated by (1, -1) = #{b.translate(1.0, -1.0).to_s}")

puts("")
puts("Circle:")
c = Circle.new(center: a, radius: 5.0)
puts("  #{c.to_s}")
puts("  area = #{c.area}")
puts("  contains b? #{c.contains(b)}")
puts("  contains (10, 10)? #{c.contains(Point.new(x: 10.0, y: 10.0))}")

puts("")
puts("Rectangle:")
r = Rectangle.new(top_left: Point.new(x: 1.0, y: 1.0), width: 6.0, height: 4.0)
puts("  #{r.to_s}")
puts("  area = #{r.area}")
puts("  perimeter = #{r.perimeter}")
